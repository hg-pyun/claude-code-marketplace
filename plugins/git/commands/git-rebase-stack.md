# Git Rebase Stack Command

Automatically cleans up stacked branches using rebase --onto with --update-refs.

## Arguments
- `$ARGUMENTS`: Optional. Pass intent in natural language.

Invoke the git:git-rebase-stack skill and follow it exactly as presented to you.

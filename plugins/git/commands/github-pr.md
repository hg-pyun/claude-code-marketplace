# GitHub PR Command

Analyzes the current branch's changes and auto-creates a GitHub Pull Request with a conventional commit-style title.

## Arguments
- `$ARGUMENTS`: Optional. Pass the `--draft` flag to create a draft PR.

Invoke the git:github-pr skill and follow it exactly as presented to you.

# Git Commit Command

Analyzes git diff to auto-generate a conventional commit message and execute the commit.

## Arguments
This command does not accept `$ARGUMENTS`. It always operates based on the current git state.

Invoke the git:git-commit skill and follow it exactly as presented to you.
